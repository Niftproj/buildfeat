// idea: build a  gui or command line utility for automated build system genration in simple words.

// $buildfeat ASSERT FIND($CC) | print()
$buildfeat USE MAKEFILE # or MESON etc
$buildfeat DECLARE "CSOURCES" WITH VALUE {FIND *.c -DEPTH 2}
$buildfeat DECLARE "CSOURCES" WITH VALUE "main.c ether.c"
$buildfeat DECLARE "C_OBJS" WITH VALUE {GEN_OBJS(.o) $CSOURCES}
// $buildfeat ROUTINE "BUILD" WITH CMDS {read(build.feat)}

// CAN BE IN TS,JS,OR ETC

// USING PYTHON
https://www.geeksforgeeks.org/command-line-scripts-python-packaging/
https://packaging.python.org/en/latest/tutorials/packaging-projects/

// It is currently on the Hold while Nift*UI is not reached at final stage.

// USING NODE.JS
https://developer.okta.com/blog/2019/06/18/command-line-app-with-nodejs